# Batch 25 Applied

- Added smoke-style tests for auth/register flow helpers.
- Added smoke-style tests for delivery/referral flow helpers.
- Added smoke-style tests for canonical client/driver route stability and controller-based legacy driver pages.
- Made `DeliveryPage.logic.js` import a pure helper module so smoke tests can load delivery logic without frontend runtime aliases.
