import React, { memo } from "react";
import SettingsScreen from "@/modules/client/features/settings/pages/Settings.jsx";

function SettingsPage() {
  return <SettingsScreen />;
}

export default memo(SettingsPage);
