import React, { useMemo } from "react";
import { Card } from "antd";
import { formatPrice } from "../../services/priceUtils";
import { useAutoMarketI18n } from "../../utils/useAutoMarketI18n";

function Sparkline({ values = [] }) { const w=260,h=70,pad=6; const min=Math.min(...values,0), max=Math.max(...values,1), span=max-min||1; const pts=values.map((v,i)=>{ const x=pad+(i*(w-pad*2))/Math.max(1,values.length-1); const y=h-pad-((v-min)*(h-pad*2))/span; return [x,y];}); const d=pts.map((p,i)=>(i===0?`M ${p[0]} ${p[1]}`:`L ${p[0]} ${p[1]}`)).join(" "); const area=d+` L ${w-pad} ${h-pad} L ${pad} ${h-pad} Z`; return <svg width={w} height={h} viewBox={`0 0 ${w} ${h}`} style={{display:"block"}}><path d={area} fill="rgba(14,165,233,.12)" /><path d={d} fill="none" stroke="rgba(14,165,233,.9)" strokeWidth="3" strokeLinecap="round" />{pts.map((p,i)=><circle key={i} cx={p[0]} cy={p[1]} r="4" fill="#22c55e" stroke="#fff" strokeWidth="2" />)}</svg>; }

export default function PriceHistoryGraph({ history = [] }) { const { am, locale } = useAutoMarketI18n(); const data=useMemo(()=> (history||[]).map(x=>({ at:x.at, price:Number(x.price||0), currency:x.currency||"UZS" })), [history]); if(!data.length) return null; const values=data.map(d=>d.price); const dateLocale = locale === 'ru' ? 'ru-RU' : locale === 'en' ? 'en-US' : 'uz-UZ'; return <Card style={{ borderRadius:18, border:"1px solid #e2e8f0" }} styles={{ body: { padding:14 } }} title={<div style={{ fontWeight:900, color:"#0f172a" }}>{am("autoExtra.priceHistory")}</div>}> <Sparkline values={values} /> <div style={{ marginTop:10, display:"grid", gap:6 }}>{data.slice().reverse().map((d,i)=><div key={i} style={{ display:"flex", justifyContent:"space-between", fontSize:12 }}><span style={{ color:"#64748b" }}>{new Date(d.at).toLocaleDateString(dateLocale)}</span><span style={{ fontWeight:900, color:"#0f172a" }}>{formatPrice(d.price,d.currency)}</span></div>)}</div></Card>; }
