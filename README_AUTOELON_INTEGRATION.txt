AUTOELON-LIKE INTEGRATION (NO CERTS/ADS SDK)
------------------------------------------------
Qo'shildi:
- market_remote_config.json: post image quality/max_images/chat flags/network timeout
- market_tiles.json: tile actions (E'lon berish / Avtomobillar / Ehtiyot qismlar)
- market_params.json: marka->model dependent, fuel, gearbox, cities
- market_ad_states.json: listing states

UI:
- AutoMarketPanel: tile buttons + Browse / Post Ad
- PostAdForm: e'lon berish formasi (rasm compression + dependent selects)
Storage:
- marketStorage.js: localStorage ga saqlaydi (test). Keyin APIga ko'chiriladi.

Sertifikat / reklama / integrity qismlari qo'shilmadii.
