import React from "react";

/**
 * Optional virtualization wrapper.
 * If `react-window` is installed, you can switch heavy lists to virtualized rendering.
 * This file is safe even if unused.
 */
export default function VirtualList({ children }) {
  return <>{children}</>;
}
