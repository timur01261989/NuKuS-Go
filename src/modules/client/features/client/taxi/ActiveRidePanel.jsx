import React from "react";

/** Active ride UI (coming/arrived/ontrip). Keep logic in parent; this is UI-only placeholder. */
export default function ActiveRidePanel({ children }) {
  return <>{children}</>;
}
